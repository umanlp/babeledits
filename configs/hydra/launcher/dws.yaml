defaults:
  - submitit_slurm

_target_: hydra_plugins.hydra_submitit_launcher.submitit_launcher.SlurmLauncher
# timeout_min: 20 # 2 hours
partition: ${oc.env:DEFPART}
mem_gb : 200
cpus_per_task: 30
gres: gpu:1
additional_parameters: {"time": "15:00:00", "mail-user": "${oc.env:USER_MAIL}", "mail-type": "ALL"}
